# =============================================================================
# Thero AI - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values as needed.
# cp docker/.env.example docker/.env
# =============================================================================

# -----------------------------------------------------------------------------
# Core Settings
# -----------------------------------------------------------------------------
NODE_ENV=development
LOG_LEVEL=info

# Security (CHANGE IN PRODUCTION!)
SECRET_KEY=your_random_encryption_secret_key

# Public URLs (set for external access)
ALLOWED_ORIGINS=
CONNECTOR_PUBLIC_BACKEND=
FRONTEND_PUBLIC_URL=

# -----------------------------------------------------------------------------
# Database Credentials
# -----------------------------------------------------------------------------
# MongoDB
MONGO_USERNAME=admin
MONGO_PASSWORD=password

# ArangoDB
ARANGO_PASSWORD=your_password

# Redis (leave empty for no password in dev)
REDIS_PASSWORD=

# Qdrant
QDRANT_API_KEY=your_qdrant_secret_api_key

# -----------------------------------------------------------------------------
# AI / LLM Configuration
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Optional Services
# -----------------------------------------------------------------------------
# Docling (document processing)
DOCLING_URL=http://localhost:8081

# Web Search (only needed with websearch stack)
SEARXNG_BASE_URL=http://searxng:8080
FIRECRAWL_BASE_URL=http://firecrawl-api:3002
MAX_LLM_CALL_PER_RUN=50

# ETCD (for websearch stack)
ETCD_URL=http://etcd:2379



