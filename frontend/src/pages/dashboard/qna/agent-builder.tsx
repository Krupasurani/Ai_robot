import React from 'react';

import AgentBuilder from 'src/sections/qna/agents/agent-builder';

export default function AgentBuilderPage() {
  return (
    <AgentBuilder
      onSuccess={() => {}}
      onClose={() => {}}
      editingAgent={undefined}
    />
  );
}


