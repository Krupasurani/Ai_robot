// Simplified theme types - no longer dependent on MUI
export type ThemeColorScheme = 'light' | 'dark';

export type ThemeDirection = 'ltr' | 'rtl';

// Legacy types for backward compatibility (can be removed once all components are migrated)
export type Theme = any;

// Typography
export const defaultFont = 'Public Sans Variable';
